# DataCrawler v2.0 (Harvest) — 진행 이력

> `PROJECT_REPORT.md`에서 분리된 작업 이력 문서입니다.
> 프로젝트 구조는 `PROJECT_REPORT.md`, 미해결 이슈·백로그는 `ISSUES.md` 참고.

- **최초 감사 일자**: 2026-07-03 ~ 2026-07-04 (조사 범위: 전체 소스 코드 약 16,200줄, 문서, Git 이력, 의존성, 보안)
- **최신 갱신**: 2026-07-15 01:45

---

## 완료된 작업 (시간순)

> 이 표의 각 행은 작업 단위(버그 수정/기능 추가/릴리스 등) 하나를 기록합니다. 새 항목을 추가할 때도 이 형식(날짜 | PR/커밋 | 항목 | 배경·원인 | 수정·내용 | 검증)을 유지합니다.

| 날짜 | PR/커밋 | 항목 | 배경·원인 | 수정·내용 | 검증 |
|---|---|---|---|---|---|
| ~07-04* | `d469277` | 리다이렉트 URL 불일치 수정 | `worker._handle_line()`이 응답의 최종 URL로 사전 생성 url_list를 대조 — 리다이렉트 사이트는 최종 URL≠요청 URL이라 전량 "URL 불일치"로 skip(total=0) | `engine.get_response_status()`에 `req_url`(리다이렉트 전 최초 요청 URL) 추가, `worker._handle_line()` 대조 기준을 `req_url`로 변경(없으면 `url` fallback) | 로컬 302 리다이렉트 서버 + `run_spider` e2e — 수정 전 skip 재현/수정 후 정상 수집 확인(PASS), Windows GUI 실수집 확인 |
| ~07-04* | `e10073c`, `3edb01a` | 저장소 전체 줄바꿈(LF) 정규화 | `.gitattributes(eol=lf)` 도입 전 CRLF로 커밋된 파일 20개 존재 | 20개 파일 LF로 일괄 정규화, "EOL 림보"(CRLF blob+LF 규칙→영구 modified 유령상태) 원인·해법을 GIT_GUIDE에 문서화 | `git diff --ignore-cr-at-eol` 기준 내용 변경 0줄 확인, main/develop 모두 LF blob 확인(재발 없음) |
| ~07-04* | PR #2, #3 | Git 운영 체계 정비 | GitHub ruleset("PR 필수")과 가이드의 직접 머지 플로우 충돌(직접 푸시 시 owner bypass 경고) | GIT_GUIDE를 PR 기반 플로우로 개정(`gh pr create`→`gh pr merge --admin`, 1인 저장소는 자기 승인 불가), EOL 노이즈 진단법 추가 | - |
| ~07-04* | PR #4 | 1차 릴리스 및 멀티환경 동기화 | - | `develop→main` 릴리스 PR 머지(main=`0155bfa`) | WSL/Windows 클론 모두 main·develop 동기화, 양쪽 working tree clean 확인 |
| ~07-04* | 이슈②, PR #5 | 프록시 rate limit 미들웨어 수정 | 수정 중 3건 발견: ①`middlewares.py:213`이 주석 처리된 `REQUESTS_PER_MINUTE` 참조→AttributeError, ②설정 키 불일치(worker는 `PROXY_REQ_INFO`에 저장, 미들웨어는 `ip_list`/`allow_ip_cnts` 조회→GUI 경로는 프록시 항상 빈 리스트), ③형식 불일치(GUI는 dict 행, 미들웨어는 URL 문자열 요구) | `customized_settings.set_ip_settings()`가 dict→URL 변환(비활성 행 제외), `worker.set_scrapy_settings()`가 올바른 키로 직접 주입, `RateLimitedProxyMiddleware`를 "여유 프록시 무작위 순회 선택"(전부 소진 시만 IgnoreRequest)로 변경 | 유닛 8건 전건 PASS(검증 후 삭제, PR #6), e2e(로컬 서버+포워드 프록시+`run_spider`) 수정 전 프록시 경유 0건→수정 후 전 요청 경유+수집 성공 |
| ~07-04* | PR #6 | 검증용 테스트 제거 | - | PR #5 검증용 미들웨어 유닛 테스트 8건을 정책(검증 산출물 미커밋)에 따라 제거 | - |
| ~07-04* | PR #7 | 문서 통합 | - | `PROJECT_GUIDE.md`+`PROJECT_AUDIT_REPORT.md`를 `PROJECT_REPORT.md`로 통합, 원본 삭제, `CLAUDE.md` 참조 갱신 | - |
| ~07-04* | 이슈④, PR #8 | 미사용 파이프라인 제거 및 기본 파이프라인 교체 | `MongoDBPipeline`이 GUI 정상 경로 어디서도 미사용(항상 `LoadItemPipeline`으로 교체), GUI DB 내보내기 UI는 파이프라인 미연결 — import 복구 대신 제거 선택 | `pipelines.py`에 `LoadItemPipeline`만 유지(`DonasPipeline`/`CsvExportPipeline`/`MongoDBPipeline`/구버전 초안 등 283줄 제거), `settings.py` 기본 `ITEM_PIPELINES`를 `LoadItemPipeline`로, 호출처 없는 `set_item_pipelines()` 제거, spiders 4종 죽은 주석 정리 | 기본 설정 크롤 e2e — 수정 전 `NameError: db_conn` 재현/수정 후 정상 추출, GUI 경로 e2e `EXECUTOR_STATUS: SUCCESS`. 검증 중 이슈⑧ 신규 발견(백로그 등록) |
| ~07-04* | PR #9 | 이슈 ⑧ 등록 및 리포트 현행화 | - | 이슈⑧(`/text()` XPath 추출 깨짐) 백로그 1순위 등록, 이슈④ 해결 처리, PR #7/#8 기록, 예외 삼킴 개선을 별도 백로그로 분리 | - |
| 2026-07-05 | PR #10 | 2차 릴리스 | - | `develop→main` 머지(main=`2dfbdcd`, PR#5~#9 포함), 이후 문서분리(PR#11) 반영 릴리스 PR#12 머지(main=`f611a65`) | Windows GUI 검증 완료 확인 후 머지 |
| 2026-07-05 | 이슈⑧, PR #13 | `/text()`·`@attr` XPath 추출 수정 | `engine.extract_data_from_root()`가 모든 노드에 `node.xpath(".")` 재호출 — 문자열 결과(text()/@attr)는 빈 값으로 조용히 유실, JSON 파싱 가능 텍스트는 parsel 1.11이 json 타입 판정해 ValueError로 전체 실패. 검증 중 `@href` 등 속성 XPath도 동일 버그 확인 | `node.root`가 문자열이면 그대로 사용, 요소 노드만 기존 xpath+태그 제거 경로 유지 | 유닛 4케이스 — 수정 전 3/4 FAIL→수정 후 4/4 PASS, e2e(`run_spider`) 수정 전 0건(SUCCESS인 채 조용한 유실)→수정 후 2행 정상 수집 |
| 2026-07-05 | PR #14 | `spiders` 키 조회 리팩터 | `get_spider()`가 최상위 `spiders` 키만 조회해 문서 §5 예시(`conditions` 내부 표기)와 불일치 | 파라미터명을 `request_info`로 정정, `conditions["spiders"]` 우선 조회+최상위 fallback(`request_info.json` 무변경) | e2e 최상위 형태 수정 전후 PASS(회귀없음), conditions 내부 형태 수정 전 FAIL(`KeyError`)→수정 후 PASS, 유닛 4케이스 PASS |
| 2026-07-05 | PR #16 | 3차 릴리스 | - | `develop→main` 머지(main=`f9fa763`, PR#13 XPath 수정·PR#14 리팩터·PR#15 문서 현행화 포함) | WSL·Windows 클론 동기화 확인 |
| 2026-07-05 | 이슈⑥, PR #19 | `get_response_status()` 방어 코드 추가 | Selenium 응답은 `response.ip_address`가 None일 수 있어 `.compressed` 접근 시 AttributeError, 비표준 상태코드(예:520)는 `HTTPStatus()`가 ValueError — 둘 다 spider의 광범위 `except Exception`에 걸려 결과가 조용히 유실 | `ip_address`는 None이면 그대로 전달, `reason`은 ValueError를 try/except로 잡아 빈 문자열 대체 | 유닛 2케이스(ip_address=None, 상태코드520) 수정 전 AttributeError FAIL→수정 후 PASS |
| 2026-07-05 | PR #20 | 4차 릴리스 | - | `develop→main` 머지(main=`9a08497`, PR#19 이슈⑥ 포함) | WSL·Windows 클론 동기화 확인 |
| 2026-07-05 | 이슈③, PR #21 | 쿠키 랜덤 미들웨어 반환값 수정 | `RandomCookieMiddleware.process_request()`가 요청에 쿠키 있으면 dict를 그대로 반환 — Scrapy `process_request` 규약(None/Response/Request만 허용) 위반으로 `_InvalidOutput` 예외 | `return request.cookies`→`return None` | 쿠키 설정된 요청 직접 전달 — 수정 전 dict 반환(AssertionError)/수정 후 None 반환 PASS |
| 2026-07-06 | 이슈⑤, PR #23 | `DelaySchedulerMiddleware` 재설계 | `settings.py`가 존재하지 않는 설정 키(`SCHEDULER_MIDDLEWARES`)에 등록해 미들웨어 자체 미로드, 내부 로직도 제거된 API(`engine.schedule()`) 호출·`process_spider_output`의 `DontCloseSpider`도 `spider_idle` 핸들러가 아니면 무효, `delay_until` 설정 호출부도 전무해 완전한 죽은 코드 | `_DelayedRescheduler` 공유 헬퍼 신설(`reactor.callLater`+`engine.crawl()`, `spider_idle`에서 `DontCloseSpider`), 등록 키를 `SPIDER_MIDDLEWARES`로 정정, `RateLimitedProxyMiddleware`도 연동(전량 소진 시 폐기 대신 지연 재시도), 재주입 시 `dont_filter=True` 적용.<br>**PR 리뷰(8앵글+verify)에서 후속 결함 2건 CONFIRMED, 병합 전 수정**: ① `CLOSESPIDER_*`가 `spider_idle` 우회로 스파이더 직접 종료→예약 타이머의 `engine.crawl()` RuntimeError가 조용히 삼켜짐 — `pending_calls` 추적+`spider_closed`에서 타이머 취소·경고 로그로 수정. ② 재스케줄에 상한 없어 무한 재스케줄 가능 — `MAX_RATE_LIMIT_RETRIES`(5회)+카운터+통계 추가 | mock 크롤러(실제 Scrapy Request/Settings+twisted reactor)로 지연 후 정상 재주입, rate limit 초과 시 재스케줄 등록 확인. 리뷰 후속: 정상 경로 회귀없음, 강제종료 시 닫힌 엔진에 crawl() 미호출, 5회 재시도 후 통계와 함께 포기 확인. `delay_until` 제자리 변경 관련 낮은 우선순위 리스크는 백로그 등록 |
| 2026-07-06 | 이슈⑦, PR #25 | 미구현 스파이더 타입 예외 처리 | `get_spider()`가 미구현 3종에 빈 dict `{}` 반환, 미인식 타입은 암묵적 None 반환 — `worker.py`가 그대로 `process.crawl()`에 넘겨 Scrapy 내부에서 불투명한 실패 | 미구현 3종은 `NotImplementedError`, 미인식 타입은 `ValueError` 명시적 raise(호출부 기존 try/except가 이미 로그+중단 처리하므로 호출부 변경 불필요) | - |
| 2026-07-06 | 이슈⑨~⑫, PR #27 | 스케줄 기능 전면 복구 | `SchedulerPage`가 `MainWindow`의 실제 `SessionSettingsPage`를 안 쓰고 별도 인스턴스 보관→스케줄 등록 시 UA/쿠키 항상 기본값+`proxy` 키 자체 없어 스케줄 수집이 프록시 미사용(⑨). 프록시 존재 여부를 구버전 스키마 `net_rotate` 키로 확인해 항상 "없음" 판정(⑩). 월간 스케줄이 ms 환산값을 `QTimer.start()`에 그대로 넘겨 int32 최댓값 초과 OverflowError(⑪). 스케줄 저장이 설치/소스 디렉터리라 PyInstaller 실행마다 유실(⑫) | 실제 `SessionSettingsPage` 인스턴스를 `SchedulerPage`에 주입, 수동 실행과 동일 스키마의 `proxy` 딕셔너리 추가, 프록시 검증도 현행 스키마(`_proxy_rows`)로 교체, 7일 단위 타이머 재등록으로 월간 오버플로우 제거, 저장/로드를 `file_path`(LOCALAPPDATA) 기준으로 변경(`BlueprintStorage`와 동일 정책) | - |
| 2026-07-06 | 이슈⑬, PR #29 | 죽은 SeleniumMiddleware 경로 제거 | `SELENIUM_DRIVER_EXECUTABLE_PATH` 항상 미설정이라 `scrapy_selenium.SeleniumMiddleware`가 매번 `NotConfigured`로 자체 비활성화, 채워도 구버전 Selenium API로 `TypeError`. 실제 렌더링은 이미 `spirenderer.py`가 전담해 죽은 의존성 자체를 제거하는 방향 선택 | `settings.py`에서 등록·설정 삭제, `engine.get_scrapy_request()`의 `html_render` 분기가 일반 `scrapy.Request` 반환, `requirements.txt`에서 `scrapy-selenium` 제거 | - |
| 2026-07-06 | 이슈⑭, PR #31 | spirenderer 드라이버 누수 수정 | `driver.quit()`이 try 블록 마지막에 있어 렌더링/추출 중 예외(빈 셀렉터 매칭 IndexError 등) 발생 시 정리 안 되고 Chrome 프로세스 누적 | 드라이버 생성 이후 코드를 내부 try/finally로 감싸 `driver.quit()`을 finally로 이동 | mock으로 추출 도중 예외 강제 발생 — `driver.quit()` 호출 확인 |
| 2026-07-06 | 이슈⑮, PR #33 | POST URL 파싱·미지원 분기 방어 | `get_json_form()`이 `?` 없는 POST URL에서 `None[0]` TypeError로 크래시, 정규식도 잘못된 이스케이프(`\?`)로 SyntaxWarning. `get_scrapy_request()`도 GET/POST 외 method거나 payload가 bool이 아니면 암묵적 None 반환→`yield None` | `?` 부재 시 명시적 ValueError, 정규식 raw string 전환, 지원 범위 밖 조합도 명시적 ValueError(모든 `start_requests()` 호출부가 이미 try/except로 감싸 조용한 실패 대신 명확한 로그로 처리) | GET/POST(formdata)/POST(json) 정상 3종+신규 예외 3종을 인터프리터에서 직접 실행 확인 |
| 2026-07-06 | PR #35 | 이슈 ⑯(다중 블루프린트 대응) 보류 처리 | blueprint 2건 이상 시 빈 설정으로 기동되는 문제 | 단건 전제인 현행 구조를 땜질 수정하는 대신 예정된 다중 블루프린트 업그레이드에서 재설계하기로 결정, `ISSUES.md`에 미해결 아닌 보류로 등록 | - |
| 2026-07-06 | 이슈⑰, PR #37 | 재시작 시 구 워커의 지연 finished 신호 무시 | 중지 직후 바로 재시작하면 구 워커 정리(최대 ~1.3s) 전에 새 워커 시작. `QThread.wait()`가 메인 스레드를 막는 동안 구 워커의 finished 신호가 큐잉됐다 새 워커 시작 후 도착 — `_on_finished`가 출처 구분 안 해 정상 수집 중인 새 워커를 "중단됨"으로 되돌림 | `_on_finished(task, summary)` 최상단에 `if self.sender() is not self._worker: return` 가드 추가 | 격리된 PyQt6 재현(구 워커 0.6s 지연 vs 신 워커 즉시 교체) — 구 워커 신호 무시, 신 워커 신호 정상 처리 확인 |
| 2026-07-06 | PR #39 | GUI 로그에서 내부 [DEBUG] 진단 메시지 제거 | URL 샘플 덤프 등 디버깅용 `[DEBUG]` 로그가 사용자 대면 로그 뷰어에 그대로 노출(내부 변수명 노출, 조치 불가), 로그 필터에도 DEBUG 등급 없어 숨길 방법 없음 | 해당 GUI 노출 emit만 제거(내부 logger 콘솔/파일 로그는 유지), 실질적 의미 있는 2건(URL 미생성, 시작 시 URL 개수)은 태그 없이 평문 로그로 유지 | - |
| 2026-07-06 | PR #41 | 수집물별 커스텀 정제 규칙 플러그인 | 사이트마다 데이터 형식이 달라 범용 정제 규칙 하나로 커버 불가 | `preprocess.load_custom_rule(seq_no)` 신설(`<앱데이터>/custom_rules/{seq_no}.py`의 `refine(data)`/`refine_row(row)` 로드, 파일 없으면 None/깨져있으면 예외 전파). `MonitorPageTriggers._run_refine()`에 배선 — `seq_no`+`needs_cleaning` 모두 있을 때만 범용 6규칙보다 먼저 적용, 실패 시 원본 폴백+로그 | - |
| 2026-07-07 | PR #42 | 커스텀 정제 규칙 경로 통일 및 미설정 경고 | 고객마다 규칙을 세트로 준비해 개별 PC에 심는 배포 구조인데, PR#41 당시 커스텀 규칙은 시딩 없이 `custom_rules/`에 두어 `request_info.json`(`BlueprintStorage`)과 다른 경로 정책 사용 | ①경로 통일 — `preprocess._app_dir()`를 `LOCALAPPDATA/CollectorApp`으로, `BlueprintStorage`와 동일한 seed-on-first-run 패턴(`_resolve_custom_rule_path`) 추가(번들 리소스→앱데이터 최초 1회 복사, 이후 앱데이터 우선). ②미설정 경고 — `preprocess.custom_rule_exists(seq_no)`(exec 없는 존재 확인) 신설, "②정제 규칙 설정" 탭 진입 시 규칙 파일 없으면 팝업 안내(`_cleaning_warned`로 반복 방지, 새 결과 유입 시 리셋) | 임시 uv venv 스크립트로 (a) 경로 해석·시딩 3케이스, (b) 헤드리스 PyQt6로 팝업 게이팅 5케이스 확인 |
| 2026-07-07 | PR #43 | 5차 릴리스 | - | `develop→main` 머지(main=`90ef5ab`, PR#41 플러그인+PR#42 경로통일/경고 포함). PR#20 이후 여러 릴리스(#22~#40)는 개별 미기록(각 PR#19~#39 항목에 이미 기록) | - |
| 2026-07-07 | `1faa6c1` | `PREPROCESS.md` 문서 신설 | PR#41/#42 커스텀 정제 규칙 플러그인의 규칙 정리·개발 프로세스 지침 필요 | `systems/PREPROCESS.md` 신설(범용 규칙 표, 커스텀 규칙 파일 규약, 개발용 `custom_rules/` 폴더 정책, 배포 워크플로), `.gitignore`에 `systems/STUDY.md` 제외 추가. 직후 정책 서술을 뺐다가(`3b6ae0b`) 되돌리는(`d638788`) 왕복 있었으나 최종 내용은 최초 커밋과 동일(net no-op) | - |
| 2026-07-07 | PR #45 | 6차 릴리스 | - | `develop→main` 머지(main=`7df06b2`, `PREPROCESS.md` 문서 추가분만 반영, 문서 전용 릴리스). 이 릴리스 이후 커밋(`b5721db` 이하)은 아직 main 미반영 | - |
| 2026-07-08 | `b5721db` | `custom_rules/` 저장소 추적 전환 | `PREPROCESS.md` §3.1a 정책에 따라 정제 규칙은 설정값이 아닌 코드로 판단, 이력 관리 필요 | `custom_rules/000000.py`(샤브올데이 정제 규칙)와 `__init__.py`를 git 추적 대상으로 전환 | - |
| 2026-07-08 | `6bd7490`~`34cb288` | 로컬 개발 환경 정비 | Windows 클론용 짝 스크립트 부재(WSL용 `git-setup-wsl.sh`만 존재), uv 재현 파일 추적 방식 미정 | `git-setup-windows.ps1` 추가. `.python-version`/`pyproject.toml`/`uv.lock`을 한 차례 추적(`6bd7490`)했다가 `requirements.txt`를 유일 의존성 출처로 유지하기 위해 추적 해제(`a084d28`)+`.gitignore` 등록(`4625fd2`). `.gitignore`의 uv 커스텀 블록·pyenv 템플릿 중복 라인 정리(`34cb288`) | - |
| 2026-07-08 | `c6bb30f` | GIT_GUIDE: 커밋/푸시/풀 전 대상 브랜치 명시 규칙 추가 | 브랜치가 명백해 보여도 실수 방지 필요 | §5와 체크리스트에 "실행 전 대상 브랜치를 사용자에게 명시적으로 보고" 규칙 추가 | - |
| 2026-07-08 | `1bfbeef`, `5cc8914`, `3a10fab` | 커스텀 정제 규칙을 `DataRefiner`의 7번째 규칙으로 승격 | PR#41 도입 당시 커스텀 규칙은 `trigger.py`가 `DataRefiner.run()` 호출 전 별도 단계로 직접 실행 — 범용 6규칙과 계층이 달라 on/off 제어·결과 반영 방식이 비대칭 | (`1bfbeef`) `DataRefiner`가 `custom_rule` 콜러블을 받아 `_step_custom_rule()`(규칙⑦, 최우선 실행)로 흡수, `RefineStats`에 필드 추가(원본 길이 불일치/예외 시 폴백), `layout.py`에 토글 체크박스 추가, `trigger.py`는 규칙 로드만 담당하도록 정리. (`5cc8914`) `PREPROCESS.md`를 "7규칙 단일 파이프라인, ⑦ 항상 먼저 실행"으로 갱신. (`3a10fab`) 경로 해석·시딩·로드 로직을 `preprocess.py`에서 `conf.CustomRuleStorage`(신설 싱글턴)로 이관, 번들 기본 경로를 실제 배치 위치와 일치하도록 수정 | - |
| 2026-07-08 | `bda1096`, `155e322` | fill_null 규칙에 사용자 지정 치환값 지원 | `fill_null` 규칙의 치환값이 `preprocess.py`에 `"—"`로 하드코딩되어 사용자 지정 불가 | `DataRefiner.__init__`에 `fill_value` 인자 추가, `_step_fill_null()`이 이 값 사용. GUI는 `drop_columns`의 "체크박스 옆 QLineEdit" 패턴 재사용 — 최종적으로 GUI 기본값은 빈 값(placeholder 안내), `DataRefiner` 직접 호출 기본값은 이 시점엔 `"—"` 유지(이후 `0cda32b`에서 통일) | 헤드리스 PyQt6 — 기본값·사용자 지정값·빈 입력 폴백 3케이스 확인. `PREPROCESS.md`/`PROJECT_REPORT.md` 동기화(`155e322`) |
| 2026-07-08 | `b6e1a95` | Before/After 비교 탭 좌우 테이블 스크롤·정렬 동기화 | `cmp_raw_table`/`cmp_ref_table`이 완전 독립 동작 — 사용자 요청으로 "같은 컬럼·방향으로만 동기화" 방식 검토 후 승인받아 구현 | 두 테이블의 `verticalScrollBar().valueChanged` 상호 연결(재귀 종료 가드), `sortIndicatorChanged`도 상호 연결해 정렬 컬럼을 이름 기반으로 매칭(대응 컬럼 없으면 무시, 행 수 정합은 비보장) | 검증 중 Qt `sortIndicatorSection()`이 미정렬 테이블에서 범위 밖 값 반환하는 버그 발견 → 상한 가드(`< columnCount()`) 추가로 수정. 헤드리스 PyQt6 6개 시나리오 PASS(스크롤/정렬 양방향, 대응 컬럼 없을 때 안전 무시, 무한 재귀 없음) |
| 2026-07-08 | `418597f` | custom_rules/000000.py 전화번호 정규화 로직 단순화 | - | 샤브올데이(seq_no=000000) `tel` 필드 정규화를 자릿수 기반 재조합에서 `")"→"-"` 단순 치환으로 교체(사용자 직접 작성) | - |
| 2026-07-08 | `0cda32b` | DataRefiner 규칙 넘버링 재배치 및 fill_value 기본값 통일 | `preprocess.py`의 순환 숫자 라벨이 `custom_rule`을 ⑦로 표기하면서도 실제로는 항상 최우선 실행 — 코드/문서 최초 열람자에게 혼란 유발(사용자 요청으로 확인 후 재배치) | 실제 실행/GUI 순서로 재배치(custom_rule=①, remove_duplicate=②, remove_null_row=③, fill_null=④, trim_whitespace=⑤, drop_columns=⑥, cast_numeric=⑦) — `preprocess.py`/`trigger.py`/`PREPROCESS.md`/`PROJECT_REPORT.md` 전체 반영. `fill_value` 기본값을 `"—"`→`""`로 통일. GUI 규칙명·설명 문구 정리(사용자 피드백 반영) | 순수 로직 회귀(`DEFAULT_RULES` 키 순서 불변, custom_rule 최우선 실행, 7규칙 순차 적용, 신규 기본값 반영), 헤드리스 PyQt6 e2e 회귀없음 — 주석·문서 전용 변경이라 실행 코드 무변경을 diff로 확인 |
| 2026-07-08 | `3b98ac5`, `5a0d665` | 커스텀 정제 규칙 체크박스 — 규칙 ②~⑤ 자동 연동 | "커스텀 정제 규칙 적용" 토글마다 함께 켜는 게 자연스러운 범용 규칙(②~⑤)을 매번 수동으로 맞춰야 하는 번거로움 — 요구사항(양방향 연동, 매번 덮어쓰기) 확정 후 구현 | (`3b98ac5`) `custom_rule` 체크박스 `stateChanged`를 `_on_custom_rule_toggled()`에 연결 — 체크 시 ②~⑤ 모두 True, 해제 시 모두 False로 강제(⑥⑦은 연동 대상 아님). (`5a0d665`, 사용자 피드백) 해제 시 ②~⑤가 같이 꺼지는 동작 제거 — 켤 때만 강제, 끌 때는 직전 상태 유지 | 헤드리스 PyQt6 — 일부 꺼둔 상태에서 재체크 시 강제로 다시 켜짐, 해제 시 상태 불변, ⑥⑦ 비영향, `_run_refine()` 회귀 없음(전 시나리오 PASS) |
| 2026-07-08 | `6943a23` | GUI 중복 알림·더미 데이터 제거 및 layout.py→trigger.py 메서드 이관 | ①완료 시 로그+팝업 중복 알림, ②`SessionSettingsPage._seed()`가 앱 시작마다 프록시 샘플 4행 항상 채움, ③"layout.py=UI 구성, trigger.py=이벤트 핸들러" 원칙을 위반하는 메서드 5개 존재 | ①정상 완료 팝업 삭제(로그로 충분, 0건 경고 팝업은 로그에 없는 안내 있어 유지 — 부수효과로 팝업이 모달이라 막던 스케줄 연속 실행이 개입 없이 이어지게 됨). ②`_seed()`와 호출부 삭제(빈 리스트로 시작). ③5개 메서드(`StatisticsPage.reload()`, `SchedulerPage._manage_schedule_task()`, 프록시 토글 3종)를 trigger.py Mixin으로 이관+죽은 코드 `load_ip_list_from_file()` 제거+orphan import 정리. 이관 중 버그 2건 발견: `request_info["callback_url"]` 전역변수 참조→`BlueprintStorage().read()`로 교체, 위젯 import 누락 수정 | 헤드리스 PyQt6 — `MainWindow()` 전체 인스턴스화(이슈⑨ 유지 포함)+`reload()` 갱신+프록시 토글 전 구간+다이얼로그 빌드 6개 시나리오 PASS |
| 2026-07-08 | `e875d76` | 수동 수집 완료 시 모니터링-Raw 탭 자동 전환 | 수집 완료 후에도 화면이 그대로 유지돼 매번 "모니터링→①Raw" 탭을 수동으로 찾아가야 함 | `_on_finished()`의 정상 완료·0건 분기에서 `task["job"]=="수동 실행"`일 때만 Raw 탭으로 자동 전환(스케줄 실행·중단 분기는 화면 유지, 무인 실행 중 갑작스런 전환 방지 위해 범위 한정) | `MultiprocessWorker.finished` 실제 emit — 수동+성공/수동+0건/스케줄+성공/스케줄+0건/중단 5개 시나리오 9개 assertion, 수동 2건만 전환됨 확인 |
| 2026-07-08 | `4f87aee` | 수집 결과 0건 완료 시 진단 정보(URL 개수·소요시간·skip 건수) 노출 | 결과 0건 시 항상 동일한 안내만 노출(원인은 URL생성실패/전량skip/전부에러/추출0건 등 다양). "URL 불일치 skip" 정보는 `worker.py`에 있었지만 `logger.warning()`만 써서 사용자에게 안 보임 | `worker.py`에 `_skipped` 카운터 추가(URL 불일치만 집계), `_emit_finished()`에 `url_count` 인자+`summary`에 `url_count`/`skipped` 필드 추가. `trigger.py`의 0건 완료 로그·팝업 문구에 "생성 URL n개·불일치 skip n건·소요 ns" 추가 | 헤드리스 PyQt6 — 판정 분기별 카운터 증감, summary 필드, 로그·팝업 반영, Raw 탭 자동 전환 회귀 없음 — 15개 assertion 확인 |
| 2026-07-09 | PR #47 | 7차 릴리스 | - | `develop→main` 머지(GitHub 웹, ruleset owner bypass, main=`c91a435`) — 커밋 15개(fill_null 치환값, Before/After 동기화, tel 정규화 단순화, 체크박스 자동연동, 규칙 넘버링 재배치, GUI 중복알림/더미데이터 제거+메서드 이관, Raw탭 자동전환, 0건 진단정보 등) | `ISSUES.md`/`PROJECT_REPORT.md` 동기화 반영 |
| 2026-07-10 | `refactor/code-cleanup`(6개 커밋) | CODING_GUIDE.md 기준 전체 코드 정리 | `CODING_GUIDE.md` 신설 후 코드베이스 전체(~10,200줄)를 가이드 기준 점검 — ruff로 기계적 위반 선별 후 LLM이 네이밍/매직넘버/중복/죽은코드 등 판단 필요 부분을 파일군 단위로 순회 | (`1128e6d`) 미사용 import·죽은코드·변수 제거+ruff --fix 77건. (`2aafc38`) ruff 미해결 41건 수동 정리(payload 3분기는 `is True`/`is False` 엄격비교 유지, bare except에 구체 타입 지정). (`c34a87a`) 소형 모듈 12개 — f-string 중첩따옴표 제거, 변수명 개선, 매직넘버 상수화(딕셔너리 키 문자열 리네임 실수 발견 후 롤백, 이후 키는 리네임 제외 규칙화). (`6a442ba`) `engine.py`/`utility.py`의 미지원 분기 else 부재→명시적 ValueError 전환, 죽은 `update_slash(reverse=True)` 제거. `worker.py`/`middlewares.py`/`db_conn.py`/`conf.py`는 이전 세션에 이미 정리돼 변경 없음. (`51fc797`) `layout.py` docstring 정정, 죽은 콜백/orphan 헤더 제거. (`122315c`) 반복 딕셔너리 리터럴 3종을 모듈 상수로 통합, `_update_step_ui()`가 완전한 no-op임을 데이터 흐름 분석으로 확인 후 제거(대체 경로 존재로 동작 무변화) | 매 커밋 `ruff check .` 전체 통과+`py_compile` 통과, 최종 헤드리스 PyQt6로 `MainWindow` 실구동 4개 시나리오 PASS. 의도적 보류: 스파이더 5종 중복(다중 블루프린트 재설계와 겹침), 대형 함수 분리(설계 판단 영역), `middlewares.py` 미사용 클래스(토글용 의도적 코드) |
| 2026-07-10 | `d6f5254` | 미사용 파일 삭제 | 전수 감사(`ISSUES.md` §5)에서 식별된 미사용 파일 3종이 계속 저장소에 남아 혼동 유발 | `frames_tmp.py`(5,796줄, layout/trigger/style 분리 리팩터 이전 구버전 모놀리스, 어디서도 import 안 됨)·`spiders/sample.py`(0바이트 빈 파일)·`env/check_ini_section.py`(git 미추적 디버그 스크립트 잔재) 삭제 | - |
| 2026-07-11 | `f54507f` | 대시보드 자동 저장 설정 이동 PR #55 되돌림 | PR#55(`3e30315`)가 자동 저장 설정을 `MonitorPage` 다이얼로그에서 `DashboardPage` "수집 설정" 카드로 옮겨 머지 | UI/UX상 혼동·배치 부적절 판단 — 별도 PR 없이 develop에 직접 revert 커밋(PR#55 이전 상태로 완전 복귀, 재설계 여부·시점 미정) | - |
| 2026-07-11 | `ec7f2bf` | 자동 저장/자동 저장 대상을 "수집 & 저장 설정" 카드로 재이동 | PR#55 revert 후 같은 기능을 다른 배치로 재검토 — 별도 "출력 설정" 카드 신설안 폐기하고 기존 카드를 "수집 & 저장 설정"으로 개명 재사용(세로 높이 불균형 문제 회피), PR#55의 "⚙상세 설정" 버튼은 중복 판단으로 미포함 | `layout.py`에 `auto_save_chk`+RAW/정제 토글 추가(정제 토글에 경고 툴팁). `trigger.py`에 `_on_auto_save_toggled`/`_on_auto_save_source_selected` 신설, `_actual_start()`가 dashboard 값을 `task["extract"]`에 반영, `_open_output_settings_dialog()`에서 auto_save 관련 UI/커밋 로직 제거(단일 위치 원칙) | 헤드리스 PyQt6 — 초기 비활성화·체크 활성화·상호배타·재비활성화·툴팁, `_actual_start()` 반영, 다이얼로그 코드 완전 제거, `MainWindow()` 회귀없음 5개 시나리오 PASS, `ruff check` 통과 |
| 2026-07-11 | PR #56 | 8차 릴리스 | - | `develop→main` 머지(`gh pr merge --admin`, main=`e412ee4`) — `ec7f2bf`/`499edb0`(자동저장 재이동) + PR#49~#55(코드정리, 모니터링 개선 3건, 문서지침, PR#55 이동+revert) 포함 | 로컬 main/develop origin 동기화 확인 |
| 2026-07-11 | `trigger.py:2108-2131` | 스케줄 실행 시 자동 저장 미적용 버그 수정 | 스케줄 등록 시 `schedule_info["auto_save"]=True`로 강제하지만, `_on_finished()`가 참조하는 `task["extract"].get("auto_save")` 위치와 달랐음. 바로 다음 줄 `schedule_info.update(common_fields)`가 `extract`를 `auto_save`/`auto_save_source` 키 없는 값으로 통째 교체 — 스케줄 완료돼도 자동 저장이 항상 스킵(실측 재현 확인). 스케줄 다이얼로그엔 이를 켜고 끌 UI 자체가 없었음 | `_apply_schedule()`의 등록·수정 분기 모두, `extract` 병합 완료 후 `target["extract"]["auto_save"]=True`/`auto_save_source="raw"`를 강제(이후 병합이 덮어쓸 수 없도록 순서 조정). `"raw"` 고정 이유는 정제 선택 UI 부재+무인실행 중 리뷰 없는 정제 적용 리스크 회피 | WSL uv venv, `_apply_schedule()` 직접 호출(저장 경로는 임시 폴더로 리다이렉트) — 신규 등록 시 값 확인, 기존 수정 시 값 유지, `_on_finished()`가 보는 최종 `extract`에서 실제 True 평가 종단 확인 3개 시나리오 PASS |
| 2026-07-11 | `a4c6375` | 스케줄 정제 자동 저장에 화면 상태 무관 고정 규칙 적용 | `_run_refine()`이 실행 순간 MonitorPage "②정제 규칙 설정" 탭의 체크박스 상태를 그대로 읽는 구조 — 무인 실행(스케줄)에서는 등록 시점과 무관한 규칙이 그대로 적용될 수 있고 확인 방법도 없는 근본 리스크 | `_run_refine()`에 `rules_override`/`skip_ui_update` 옵션 추가(override 시 화면 상태 미참조, skip_ui_update 시 결과 갱신·탭 전환 건너뜀). 모듈 상수 `SCHEDULED_REFINE_RULES`(①~⑤=True, ⑥⑦=False, "①커스텀 정제 규칙 적용"과 동일 조합) 신설. `_on_finished()`가 `auto_save_source=="refined"`+`job=="스케줄 실행"`일 때만 이 고정 규칙으로 선실행 | 헤드리스 PyQt6 — 체크박스를 override와 반대로 세팅해도 고정 규칙만 적용, 탭전환/갱신 스킵, 기존 무인자 호출 회귀없음, `_on_finished()` 스파이로 (스케줄+정제)만 override 경로 확인 3개 시나리오 PASS. 잔여 리스크(빈 데이터 경고 모달이 스케줄+정제저장 조합에서 이론상 도달 가능)는 이슈⑱로 등록(현재 도달 불가, 보류) |
| 2026-07-11 | `e91c676` | 스케줄 등록 시 자동 저장 대상(RAW/정제) 선택 기능 추가 | 고정 규칙(`SCHEDULED_REFINE_RULES`) 적용으로 리스크가 해소돼, 스케줄마다 RAW/정제 선택을 노출해도 안전하다고 판단(종전엔 선택 UI 자체가 없어 `auto_save_source`가 `"raw"` 하드코딩) | `_manage_schedule_task()`에 RAW/정제 `TagButton` 쌍 추가(정제 버튼에 고정 규칙 적용 안내 툴팁), 등록/수정 모드 모두 기존 선택값 복원. `_apply_schedule()`의 하드코딩된 `"raw"` 두 곳을 토글 상태로 교체(`auto_save` 자체는 계속 강제 True 유지) | 스모크 테스트(다이얼로그 예외없이 빌드), `_apply_schedule()` 직접 호출로 RAW/정제 선택 반영·수정 모드 반대값 변경·기존 선택값 복원 확인 4개 시나리오 PASS |
| 2026-07-12 | `99144ed` | html_render 커스텀 수집/로그인 로직을 custom_rules 플러그인으로 이전 | 000010(맥도날드)·000013(네이버) 전용 렌더링/로그인 로직이 `engine.py`에 seq_no로 하드코딩돼 있어, 신규 고객마다 엔진 코드를 직접 수정해야 하는 구조였음 | `conf.CustomRuleStorage`를 `CustomModuleStorage`로 확장 — 정제(`refine`/`refine_row`)뿐 아니라 수집 단계 `render()`/`login()` 훅도 같은 메커니즘으로 로드. 존재 확인은 AST 기반 `has_refine()`/`has_render()`/`has_login()`으로 세분화(exec 없이 안전). `engine.py`의 seq_no 하드코딩 `run_login()`/`get_render_result()` 제거(관련 미사용 import 정리). `spirenderer.py`가 `conditions.login`/`rendering` 값에 따라 `CustomModuleStorage`에서 동적 로드하도록 변경(미설정 시 html 스파이더와 동일한 범용 root/items 추출로 폴백). 기존 000010/000013 하드코딩 로직은 `custom_rules/000010.py`·`000013.py`로 그대로 이전(동작 동일) | - |
| 2026-07-13 | `53978d0` | custom_rules render/refine 서브폴더 분리 및 JS 렌더링 폴백 개선 | 수집(Selenium 자식 프로세스)과 정제(메인 GUI 프로세스)가 같은 평면 폴더를 공유해 실행 컨텍스트가 물리적으로 분리돼 있지 않았음 | `CustomModuleStorage`에 `kind`(render/refine) 파라미터 도입해 물리적으로 다른 파일/폴더에서 로드하도록 변경, `custom_rules/000000.py`(샤브올데이 정제)를 `custom_rules/refine/000000.py`로 이관. `spirenderer.py`: `rendering=True`인데 `custom_rules/render/{seq_no}.py`에 `render()`가 없어도 에러로 중단하지 않고 범용 root/items 추출로 폴백(커스텀 인터랙션은 있을 때만 쓰는 예외 경로라는 방향), 폴백 시 로그 없음·커스텀 render() 실사용 시에만 info 로그 | 작업 중 발견한 미해결 이슈 2건을 `ISSUES.md`에 기록 — 이슈⑳(000010/000013 원본이 render/로 재이관 안 됨)·이슈㉑(`conditions["login"]` 직접 접근이 로그인 키 생략한 신규 request_info.json과 스키마 불일치로 KeyError, uv venv 실행 재현 확인) — 둘 다 후속 커밋에서 해결(아래 참고) |
| 2026-07-13 | `f834f9b`, `f0104d8` (PR #62, 이슈㉑) | 로그인 미사용 시 login 키 삭제되어 spirenderer KeyError 유발하던 버그 수정 | `generator_conditions.html`의 `generatePattern()`이 출력 직전 null 필드를 지우는 목록(`mainUrl`/`mainFormat`/`headers`/`params`/`items`)에 `login`도 포함 — 로그인 미사용 시 `"login": null`이 아니라 키 자체가 삭제됨. `spirenderer.py`의 `conditions["login"]` 직접 접근(`.get()` 없음)은 "로그인 없는 사이트도 `login: null`을 명시한다"는 암묵적 스키마 전제였음 | 코드 수정(방어적 `.get()`) 대신 기존 관례를 유지하는 쪽으로 결정 — `generatePattern()`의 delete-if-null 목록에서 `'login'` 제외, 로그인 미사용 시에도 `"login": null`이 항상 명시적으로 출력되도록 수정. `spirenderer.py`는 변경 없음 | delete-if-null 로직을 Python으로 재현해 수정 후 `login` 키가 유지됨을 확인. `ISSUES.md`에 이슈 3건(㉒㉓㉔) 신규 등록 및 이슈㉑ 해결 반영 |
| 2026-07-13 | `8543e1e` (PR #63) | 문서 폴더명 `systems/` → `guidelines/` 변경 | 문서 폴더명이 실제 성격(가이드라인)과 맞지 않는다는 판단 | 모든 문서 파일을 `systems/`에서 `guidelines/`로 이동, `CLAUDE.md`의 참조 경로 갱신 | - |
| 2026-07-13 | `16452f1` (PR #64, 이슈⑳·㉕) | PyInstaller 배포 파이프라인 신설 및 커스텀 규칙 render/ 서브폴더 복구 | exe 배포 시 정제 규칙 번들 여부를 검토하던 중 `.spec`/빌드 스크립트가 저장소에 전무해 `--add-data` 구성이 전적으로 수동·비문서화임을 확인(이슈㉕). `PREPROCESS.md`도 render/refine 서브폴더 분리(`53978d0`) 이전 평면 경로 기준이라 실제 `resolve_path()`와 어긋남. 같은 조사 중 맥도날드(000010)·네이버(000013) 원본이 삭제만 되고 재이관 안 된 상태도 확인(이슈⑳) | (이슈⑳) `git show 53978d0^:...`로 원본 복원해 `custom_rules/render/000010.py`·`000013.py`로 재이관(경로 주석만 수정, 로직 원본 유지). (이슈㉕) `build-exe.ps1` 신설 — `-SeqNo`와 `request_info.json`의 실제 `seq_no` 일치 검증 후 불일치 시 중단, 지정 seq_no 파일만 임시 스테이징 폴더에 모아 그 폴더만 `--add-data`(다른 고객 규칙 유출 차단), `request_info.json`도 함께 번들. `PREPROCESS.md` §3.1/§3.1a/§4/§5를 새 경로+`build-exe.ps1` 기준으로 재작성, 클래스명 `CustomRuleStorage`→`CustomModuleStorage`로 정정. 후속(`06f230c`)으로 `PROJECT_REPORT.md`의 `conf.py` 설명도 `CustomModuleStorage` 기준으로 동기화 | 이 환경(WSL/Linux)에서는 실제 exe 빌드 검증 불가 — 코드 분석 기반 작성, Windows 개발 환경에서 실행 검증 필요(한계로 명시). Scrapy/Selenium 등은 최초 빌드 시 `--hidden-import`/`--collect-all` 추가 조정 필요할 수 있음 |
| 2026-07-13 | `823e0d9` (PR #65, 사용자 실사용 중 리포트) | `build-exe.ps1` 인코딩 미지정으로 한글 깨짐/JSON 파싱 실패 수정 | Windows PowerShell `Get-Content`가 `-Encoding` 미지정 시 시스템 기본 코드페이지(한글 Windows는 CP949)로 읽음 — `request_info.json`은 UTF-8 저장이라 한글 필드 손상+`ConvertFrom-Json` 파싱 자체 실패. 실사용자가 `.\build-exe.ps1 -SeqNo 000000` 실행 중 seq_no 검증 단계에서 멈추는 것을 재현·리포트 | `Get-Content $requestInfoPath -Raw -Encoding UTF8`로 인코딩 명시 | - |
| 2026-07-13 | `8958b41` (PR #66, 사용자 실사용 중 리포트) | build-exe.ps1이 Scrapy 엔진/스파이더 설정을 번들하지 못하던 문제 수정 | 배포된 exe로 수집 시 Scrapy가 정상 동작하지 않는다는 실사용 리포트 조사 결과, PyInstaller 번들에서 3가지가 누락: ①`pipelines.py`/`middlewares.py` — `settings.py`의 `ITEM_PIPELINES`/`DOWNLOADER_MIDDLEWARES`가 문자열 경로(`"pipelines.LoadItemPipeline"`)로 런타임에 동적 import돼 정적 import가 없는 PyInstaller 자동 분석이 놓침, ②`scrapy.cfg`/`settings.py` — `get_project_settings()`의 파일 탐색도 정적 import로는 안 잡힘, ③`combine-harvester.ico` — `layout.py`가 트레이 아이콘으로 런타임에 파일 경로 직접 읽음(`--icon`과는 별개로 데이터로도 필요) | 이전에 실제 동작했던 v0.7.0 빌드 명령(사용자 제공)을 참고해 위 파일들을 `--add-data`에 추가, `--copy-metadata` 목록도 함께 추가(Scrapy/Twisted 계열이 `importlib.metadata`로 패키지 정보 조회 — PyInstaller가 기본적으로 메타데이터를 담지 않아 freeze 시 흔히 깨지는 문제의 표준 해결책). `spiders/`는 `engine.py`의 정적 import로 이미 자동 번들되지만 안전하게 함께 유지. 옛 파일 `collect_info.pkl`(request_info.json으로 대체됨)은 제외 | - |
| 2026-07-13 | `0d876be` (PR #67, 사용자 실사용 중 리포트) | frozen exe에서 multiprocessing.freeze_support() 누락으로 수집이 조용히 0건 처리되던 문제 수정 | 배포 exe로 수집 시 에러 없이 결과만 0건 나온다는 리포트 조사 결과: PyInstaller onefile exe + `multiprocessing.Process(worker.run_spider)` 조합에서 Windows 필수 보일러플레이트 `multiprocessing.freeze_support()`가 호출되지 않고 있었음 — 없으면 자식 프로세스가 `__main__`을 처음부터 재실행해 GUI를 한 번 더 띄우려다 `QLocalServer` 단일 실행 감지("이미 실행 중입니다")에 걸려 즉시 종료. `run_spider()`가 실제로 호출된 적이 없어 크롤링 자체가 실행되지 않지만, 부모 프로세스 입장에선 "출력 없이 빠르게 끝난 자식 프로세스"로만 보여 에러 없이 0건 처리됨(일반 `.py` 실행 시엔 no-op이라 지금껏 미발견). 별도로 `get_project_settings()`의 `scrapy.cfg` 탐색이 `os.getcwd()` 기준이라 frozen exe(CWD=실행 위치)에서는 번들된 `scrapy.cfg`를 못 찾아 커스텀 `settings.py`가 조용히 무시될 수 있는 지점도 함께 발견 | `main.py`의 `if __name__ == "__main__"` 블록 최상단에 `multiprocessing.freeze_support()` 추가. `worker.set_scrapy_settings()`에서 `os.environ["SCRAPY_SETTINGS_MODULE"]`과 `sys.path`를 직접 설정해 `scrapy.cfg` 파일 탐색을 우회 | - |
| 2026-07-13 | `63178d7` | 불필요한 Scrapy Telnet Console 비활성화로 방화벽 알림 제거 | 수집마다 Scrapy가 자동으로 여는 디버그용 Telnet Console(127.0.0.1:6023)이 Windows 방화벽 허용 알림을 유발 — 이 GUI 앱은 콘솔 접속 기능을 전혀 쓰지 않는데도 `TELNETCONSOLE_ENABLED = False` 설정이 주석 처리된 채 방치돼 있었음. 배포용 exe에서 고객이 수집할 때마다 방화벽 알림을 보면 프로그램을 의심할 수 있다는 우려 | `settings.py`에 `TELNETCONSOLE_ENABLED = False` 명시 활성화 | - |
| 2026-07-13 | `de6e7e6`, `38981aa` (PR #69, #70) | GIT_GUIDE.md 전체 흐름 도식 추가 및 영문화 | 브랜치 전략·커밋 체크포인트·PR 플로우·다중 환경 동기화를 한눈에 보여주는 다이어그램 부재 | (`de6e7e6`) GIT_GUIDE.md 최상단에 ASCII 다이어그램(섹션 0) 추가. (`38981aa`) 문서 전체가 영어로 작성돼 있어 다이어그램의 한글 텍스트를 영문으로 통일 | - |
| 2026-07-13 | PR #71 | 9차 릴리스 | - | `develop→main` 머지(main=`a772361`) — PR #57~#70 전체 포함(스케줄 자동저장 버그 수정 3건, render/refine 플러그인 아키텍처 도입, 로그인 키 버그 수정, 문서 폴더명 변경, PyInstaller 배포 파이프라인 신설+버그 수정 3건, GIT_GUIDE 다이어그램) | - |

\* 원문에 날짜가 명시되지 않아 최초 감사 기간(2026-07-03~07-04, 다음 명시적 날짜인 PR #10의 2026-07-05 이전)으로 추정한 값입니다.

---

## 현재 브랜치 상태 (2026-07-15 기준)

| 브랜치 | 커밋 | WSL | Windows |
|---|---|---|---|
| `main` | `a772361` (PR #71) | ✅ | 미확인 |
| `develop` | `f4c4939`(로컬 ref, PR #71 병합 커밋 자체만 미반영 — 내용상 main과 동일) | ✅ | 미확인 |

`main`/`develop`이 PR #71 릴리스로 동기화됨 — 스케줄 자동저장 버그 수정,
render/refine 플러그인 아키텍처 도입, 로그인 키 버그 수정, 문서 폴더명
변경(`systems/`→`guidelines/`), PyInstaller 배포 파이프라인 신설 및
버그 수정 3건, GIT_GUIDE 다이어그램까지 모두 포함. 미실시 release PR 없음.

미결 사항: `git-setup-windows.ps1` untracked 건은 `6bd7490`으로 해소됨.
